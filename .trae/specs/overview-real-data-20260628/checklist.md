# Checklist - 资产总览页面数据真实性改造

## 前端API层
- [x] `fetchState()` 函数正确调用 `/api/state` GET
- [x] `saveState(state)` 函数正确调用 `/api/state` PUT
- [x] `fetchAssets()` 和 `fetchOverview()` 正常工作

## 前端页面功能
- [x] 页面加载时显示加载状态
- [x] 移除所有 mock 数据变量
- [x] 时间筛选器正常工作 (日常/月统计/年统计/自定义切换)
- [x] 月份选择器正常工作 (本月/上月/4月等)
- [x] 切换时间筛选后所有数据联动更新

## 真实数据展示
- [x] 负债卡片显示从 `/api/state` debts 计算的真实数据
- [x] 进度目标卡片显示从 `/api/state` overviewGoals 获取的真实数据
- [x] 流动性指标显示从 accounts 和 records 计算的真实数据
- [x] 收入占比饼图显示从 records 计算的真实数据
- [x] 支出占比饼图显示从 records 计算的真实数据
- [x] 资产增长趋势显示真实数据（从 assetClasses 或 records 推导）

## 按钮功能
- [x] 刷新数据按钮点击后重新加载所有数据
- [x] 刷新数据按钮显示加载状态
- [x] 编辑进度目标按钮点击后打开编辑弹窗
- [x] 编辑弹窗显示当前目标值
- [x] 编辑弹窗可修改目标值
- [x] 编辑弹窗保存后调用API并更新显示
- [x] 编辑弹窗取消后关闭不保存

## 错误处理
- [x] API请求失败时显示友好错误提示
- [x] 错误状态下显示重试按钮

## 响应式布局
- [x] 桌面端 (>= 1024px) 布局正确
- [x] 平板端 (768px - 1023px) 布局正确
- [x] 移动端 (< 768px) 布局正确
- [x] 图表在各尺寸下可读

## 构建验证
- [x] 前端 `npm run build` 成功
- [ ] 后端服务正常启动
- [ ] 无控制台错误
