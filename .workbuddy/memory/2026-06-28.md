# 2026-06-28 工作日志

## 项目结构优化（assert 跨平台资产管理系统）

### 完成的优化项

1. **config/index.js 硬编码路径修复**
   - 移除 `HK_IPO_DATA_SOURCES`（13条港股数据源配置）到独立文件 `data/hk-ipo-sources.js`
   - 修复 `HK_IPO_SOURCE_FILE` 的 Windows 绝对路径硬编码，改为基于项目相对路径（`tools/build_hk_ipo_current.mjs`）
   - 更新 `services/hkipo-service.js` 的 import 路径

2. **数据库迁移机制（migrations）**
   - 创建 `db/migrate.js` 迁移运行器（读取 `schema_migrations` 表，顺序执行未运行的迁移）
   - 创建 `db/migrations/` 目录，包含 3 个迁移文件：
     - `001_add_user_settings_columns.js` — user_settings / feedback 新字段
     - `002_add_finance_asset_columns.js` — finance_assets 持仓明细字段
     - `003_add_sync_columns.js` — 多端同步字段（sync_version / deleted_at / origin_device_id / client_op_id）
   - 重构 `db/index.js`，用 `runMigrations(pool)` 替代原来的 30+ 条内联 ALTER TABLE

3. **CODE_WIKI.md 密码示例修复**
   - `MYSQL_PASSWORD=123456` → `<your-mysql-password>`
   - `ADMIN_PASSWORD=admin123` → `<your-admin-password>`
   - 添加安全提示框，警告禁止使用弱密码
   - 更新「默认账号」章节，强调必须修改默认密码

4. **创建 .env.example 模板文件**
   - 包含所有环境变量，用 `<placeholder>` 替代真实值
   - 添加安全注释（勿提交 .env 到 Git）
   - 确认 `.env` 已在 `.gitignore` 中

5. **根 package.json 依赖整理**
   - 移除重复的 `mysql2`（assert_PLATFORM/package.json 中已有）
   - 将 `better-sqlite3` 从 dependencies 移到 devDependencies（仅 migrate_to_mysql.cjs 一次性脚本使用）

### 关键决策

- 前端方向：用户选择推进 **React/Vite**（`src/` 为主线），逐步替代 `app.js` 生产
- 数据库迁移设计：采用 `schema_migrations` 表 + 编号迁移文件，支持幂等执行

### 待跟进

- React/Vite 前端完成度评估（`src/` 目前页面还是壳子，需要制定接管计划）
- CODE_WIKI.md 中「单文件后端」描述已过时（server.js 已模块化），需要更新文档
- microservices/ 和 lingua-hub/ 目录来源不明，建议在文档中说明
