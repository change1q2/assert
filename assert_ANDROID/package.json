{
  "name": "assert-android",
  "version": "2.0.0",
  "private": true,
  "description": "Assert Android client",
  "scripts": {
    "cap:add": "npx cap add android",
    "cap:sync": "npx cap sync android",
    "build:apk": "cd android && gradlew.bat assembleDebug",
    "build:aab": "cd android && gradlew.bat bundleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
